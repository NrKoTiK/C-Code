The new concept or skills i learnt are, usage of header files, header gaurds, difference between private and public objects, understanding of debugging and function calling, understanding of data manipulation, understanding the usage of iostream and fstream objects, understanding the usage of operator functions and similiar functions of such resort, understanding inheritance and polymorphismn. C and C++ share the same concepts when it comes to a programming langauge the first concepts of C such as scanf and printf got turned into std::cout and std::cin. C++ also added new features such as iostream, fstream, strings and etc. This made coding in the langauge a lot easier then it was to code in C. although C and C++ are similiar in some concepts C is no where near object oriented as C++. The object oriented concept of C++ made coding a lot simpiler as there are built in classes that are able to assist when it comes to reading files or reading data inputted. And the overall syntax of C++ is just a lot more better and normal (C coding syntax is legit gibbirsh your telling me to print some int i need to specify with "%i"??? -This took a while for me to get my head wrapped around).

Overall I didn't actually face any major issues. But during MS3 I did face an issue where the data stored within the object would disappear. After debugging with Professor Cornel it was just faulty implementation and the fixes were having to include the "const" indenttifier on the required functions. The strategies I used were using the Visual Studio debugger and help from Sir Cornel