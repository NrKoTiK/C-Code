This workshop was something. The first 2 testers were pretty okay and made me feel really good about the workshop, this all changed as soon as i got to tester_3. tester_3 really had my hair in a knot as the run function talks to a lot of functions from different classes and basically made me change a lot of my old code, same with the lineManager constructor. Couple things i learnt were that you dont need to delete a pointer unless it is dynamic memory deallocation is not handled by the coder. I also learnt differences between .front() and .begin() as these have same outcome but perform differently. as .begin() returns the first value in a iterator format and .front() just returns the object. Overall I learnt a lot when making this workshop about STL containers, Algorithms, filestream objects, string objects and reading through a string buffer.